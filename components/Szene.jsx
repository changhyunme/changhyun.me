"use client";

import { useEffect, useRef } from "react";

const Szene = () => {

    return (
        <div className="fixed top-0 left-0 w-screen h-screen bg-black hidden md:block
                        bg-[url('/stacked-steps-haikei.svg')] bg-cover bg-center
        ">
            
        </div>
    );
}

export default Szene;